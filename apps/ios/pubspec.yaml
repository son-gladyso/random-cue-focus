name: random_cue_focus
description: A privacy-first, evidence-informed goal check timer for iOS.
publish_to: "none"
version: 0.2.0+2

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  focus_core:
    path: ../../packages/focus_core
  flutter_timezone: ^5.1.0
  flutter_local_notifications: ^21.0.0
  shared_preferences: ^2.5.5
  timezone: ^0.11.1
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
